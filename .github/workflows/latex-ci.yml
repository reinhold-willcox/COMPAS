name: Build LaTeX document
on: 
    push:
        branches: [latex_compile_CI]

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - run: echo "Hello World!"

    build_latex:
        runs-on: ubuntu-latest
        steps:
            - name: Set up git repo
              uses: actions/checkout@v2

            - name: pdflatex main
              uses: dante-ev/latex-action@latest
              with:
                  working_directory: docs/COMPAS_LaTeX
                  root_file: main.tex
                  compiler: pdflatex
                  args: -interaction=nonstopmode -shell-escape

            - name: bibtex main
              uses: dante-ev/latex-action@latest
              with:
                  working_directory: docs/COMPAS_LaTeX
                  root_file: main.tex
                  compiler: bibtex 
                  args: 

            - name: pdflatex main
              uses: dante-ev/latex-action@latest
              with:
                  working_directory: docs/COMPAS_LaTeX
                  root_file: main.tex
                  compiler: pdflatex
                  args: -interaction=nonstopmode -shell-escape

